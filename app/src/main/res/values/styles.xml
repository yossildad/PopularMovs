<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/popmov_headlines</item>
        <item name="android:textColor">@color/popmov_text</item>
    </style>

</resources>
